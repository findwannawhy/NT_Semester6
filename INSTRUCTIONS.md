# Вариант 5 — Одновременная двунаправленная передача файлов по RS232C

Два равноправных ПК, соединённых нульмодемным кабелем RS232C.
**Каждый ПК одновременно является и отправителем, и получателем.**
Скорость обмена и параметры COM-порта выбирает пользователь каждого ПК при запуске.
Передача защищена **[7,4]-кодом Хэмминга** (SECDED: коррекция 1-битовых, обнаружение 2-битовых ошибок).

---

## Тестирование на одном ПК с виртуальными COM-портами (com0com)

### Шаг 1 — Установить com0com

1. Скачать **com0com** с https://sourceforge.net/projects/com0com/files/com0com/2.2.2.0/  
   Файл: `com0com-2.2.2.0-x64-fre-signed.zip`
2. Установить (требуются права администратора)
3. Открыть **Setup Command Prompt** (Пуск → com0com → Setup Command Prompt)
4. Выполнить:
   ```
   install PortName=COM1 PortName=COM2
   ```
5. Убедиться, что в Диспетчере устройств появились `COM1` и `COM2`

### Шаг 2 — Собрать проект

Открыть `KR.sln` в Visual Studio 2022 → **Ctrl+Shift+B**

Или через .NET SDK:
```cmd
dotnet build KR_KSN.csproj
```

Исполняемый файл: `bin\Debug\net8.0-windows\KR_KSN.exe`

---

### Шаг 3 — Запустить первый экземпляр (левый ПК)

1. Запустить `KR_KSN.exe`
2. В диалоге «Настройка COM-порта» выбрать:
   - Порт: **COM1**
   - Скорость: **9600**
   - Остальные: 8 бит, None, 1 стоп-бит
3. Нажать **«Открыть порт»**
4. В главном окне нажать **«Подключить»**
5. В журнале появится: `→ UPLINK`  
   Статус: **«Не подключено»** (ждёт второй стороны)

### Шаг 4 — Запустить второй экземпляр (правый ПК)

1. Запустить ещё один `KR_KSN.exe`
2. В диалоге выбрать:
   - Порт: **COM2**
   - Скорость: **9600** (должна совпадать!)
   - Остальные: 8 бит, None, 1 стоп-бит
3. Нажать **«Открыть порт»**
4. В главном окне нажать **«Подключить»**
5. В обоих окнах статус изменится на **«Подключено»** (зелёный)

---

### Шаг 5 — Передача файлов

Поскольку оба ПК равноправны, **каждый** может:

1. Нажать **«Обзор...»** → выбрать файл для отправки
2. Нажать **«▶ Отправить файл»**
3. Наблюдать прогресс в прогресс-баре и журнале
4. Когда передача завершена — нажать **«Сохранить»** на принимающей стороне

**Оба ПК могут отправлять файлы одновременно** — передачи не мешают друг другу.

---

### Демонстрация кода Хэмминга

В панели **«Отправка файла»** есть комбо-бокс **«Режим ошибок»**:

| Режим | Поведение |
|-------|-----------|
| Нет ошибок | Нормальная передача |
| 1 бит | Один бит переворачивается в каждом блоке; Хэмминг исправляет автоматически |
| 2 бита | Два бита переворачиваются; SECDED обнаруживает ошибку → `RET_CHUNK` → повтор |

---

## Структура протокола

```
Каждый кадр:  [0xFF][ТипКадра][полезная нагрузка...]

Типы кадров:
  UPLINK / ACK_UPLINK / RET_UPLINK   — установка соединения
  DOWNLINK / ACK_DOWNLINK            — разрыв соединения
  FILE_INFO / ACK_FILE_INFO          — метаданные файла
  CHUNK / ACK_CHUNK / RET_CHUNK      — передача блоков с Хэммингом
  TRANSFER_DONE / ACK_DONE          — завершение передачи
```

Шифрование блоков: **[7,4] Хэмминг + бит общей чётности** (SECDED).  
Каждый входной байт → 2 выходных байта (4 бита → 8 битовое кодовое слово).

---

## Тестирование на двух физических ПК

1. Подключить нульмодемный кабель RS232C
2. На каждом ПК запустить `KR_KSN.exe` и указать **одинаковые параметры порта** (скорость, биты, чётность)
3. На каждом ПК нажать **«Подключить»** — соединение установится автоматически
4. Передавать файлы в обоих направлениях одновременно
